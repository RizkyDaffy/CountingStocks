# Docker build cache — never copy these into the image
node_modules
**/node_modules
dist
dist-ssr
.output
.vinxi
.tanstack
.nitro
.wrangler
.git
.gitignore
.iris-tmp
brain
*.log
npm-debug.log*

# Unrelated services (not part of this image; google-sheet carries ~330MB node_modules)
microservices/google-sheet

# Secrets — never bake into image layers
.env
.env.local
.env.*.local
.dev.vars

# Dev tooling
.vscode
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db
